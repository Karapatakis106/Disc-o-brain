#! bin/bash

echo "Input your address 1,x,x:"
    read address
LXTerminal dialog --title "Select an ISO image.." --fselect /path/to/dir height width echo "${FILE} Image Chosen!." && cdrecord -v -dao dev=$address file.iso && echo "Process Done! " && sleep 6;